syntax = "proto3";

package entity;

// Specify the Go package name
option go_package = "./entity";

enum EntityType {
    ENTITY_TYPE_NONE = 0;  // No entity type specified. enum always has first entity type = 0
    ENTITY_TYPE_OTP = 1;
	ENTITY_TYPE_USER = 2;
	ENTITY_TYPE_SESSION = 3;
}